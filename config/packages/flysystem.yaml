flysystem:
    storages:
        image.storage:
            adapter: 'local'
            options:
                directory: '%kernel.project_dir%/var/storage/images'
        file.storage:
            adapter: 'local'
            options:
                directory: '%kernel.project_dir%/var/storage/files'